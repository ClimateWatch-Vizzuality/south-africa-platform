apiVersion: v1
kind: Service
metadata:
  namespace: climate-watch
  labels:
    name: south-africa-platform
  name: south-africa-platform
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    name: south-africa-platform
